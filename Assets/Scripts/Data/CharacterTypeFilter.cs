namespace Solis.Data
{
    /// <summary>
    /// Used to filter characters by type.
    /// </summary>
    public enum CharacterTypeFilter
    {
        Human = 1,
        Robot = 2,
        Both = 3
    }
}